<Window x:Class="Fun_Dub_Tool_Box.ProcessingQueueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fun_Dub_Tool_Box"
        mc:Ignorable="d"
        Title="ProcessingQueueWindow" Height="450" Width="800">
    <Grid>
        <Grid Grid.Row ="1" Margin="0,10,0,10">
            <Grid Grid.Row="0">
                <Grid>
                    <Border Background="#FF4D4D4D" VerticalAlignment="Top" Grid.Row="0">
                        <TextBlock x:Name="SequentialProcessingTextBlock" Text="Sequential Processing" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="FiraSansBold" Margin="0,0,0,0" HorizontalAlignment="Center" />
                    </Border>
                    <ListView Grid.Row="1" x:Name="SequentialProcessingListView" Background="#FF333333" FontFamily="PTSansRegular" ItemsSource="{Binding SequenceData}"  d:ItemsSource="{d:SampleData ItemCount=5}"  VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="0,30,0,0" Foreground="White">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn x:Name="NumberHeader" Header="#" DisplayMemberBinding="{Binding SequenceID}"/>

                                <GridViewColumn x:Name="FileNameHeader" Header="Name" DisplayMemberBinding="{Binding SequenceFileName}"/>
                                <GridViewColumn x:Name="SequenceFileNameHeader" Header="Path" DisplayMemberBinding="{Binding SequenceFilePath}"/>

                                <GridViewColumn x:Name="SequenceDeleteHeader" Header="Delete">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Name="SequenceDeleteBtn" BorderThickness="0" Background="#FF333333" Foreground="White" HorizontalAlignment="Left" BorderBrush="{x:Null}">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xe74d;" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Grid>
        </Grid>

    </Grid>
</Window>
