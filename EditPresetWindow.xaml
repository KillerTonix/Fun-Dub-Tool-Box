<Window x:Class="Fun_Dub_Tool_Box.EditPresetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fun_Dub_Tool_Box"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        xmlns:controls="clr-namespace:ModernWpf.Controls;assembly=ModernWpf.Controls"        
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        Title="EditPresetWindow" Height="450" Width="800">
    <Grid Background="#FF1F1F1F" >
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Row="0" Grid.Column="0" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="NaN">
                <ComboBoxItem Content="Preset 1"/>
                <ComboBoxItem Content="Preset 2"/>
                <ComboBoxItem Content="Preset 3"/>
            </ComboBox>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Content="💾 Save" Width="100" Margin="10"/>
                <Button Content="❌Delete" Width="100" Margin="10"/>
            </StackPanel>
        </Grid>
        
        
        <Grid Grid.Row="1" Margin="10">
            <TabControl>
                <TabItem Header="🎞 Video">
                    <Grid Background="#7F000000">
                        <Label Content="Video Settings (will filled in the future)" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                    </Grid>
                </TabItem>
                <TabItem Header="🔊 Audio">
                    <Grid Background="#7F000000"/>
                </TabItem>
                <TabItem Header="⚙️ General">
                    <Grid Background="#7F000000"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
