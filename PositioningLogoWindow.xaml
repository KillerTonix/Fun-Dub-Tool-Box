<Window x:Class="Fun_Dub_Tool_Box.PositioningLogoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fun_Dub_Tool_Box"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        xmlns:controls="clr-namespace:ModernWpf.Controls;assembly=ModernWpf.Controls"        
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        Title="PositioningLogoWindow" Height="720" Width="1280" Loaded="Window_Loaded" PreviewMouseWheel="Window_PreviewMouseWheel" KeyUp="Window_KeyUp">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Materials/Images/CheckerBackgroundImage.jpg" Opacity="0.5"/>
        </Grid.Background>


        <ScrollViewer x:Name="scrollViewer" MouseMove="ScrollViewer_MouseMove" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="CanvasGrid" ScrollViewer.HorizontalScrollBarVisibility="Visible">
                <Canvas Grid.Row="0" Name="myCanvas">
                    <Image x:Name="LogoImage"
                        Canvas.Left="{Binding XPosition}"
                        Canvas.Top="{Binding YPosition}"
                        MouseDown="Feast_MouseDown"
                        MouseUp="Feast_MouseUp"
                        Source="/Materials/Images/Logo3.png"                      
                        MouseMove="Feast_MouseMove"
                        Width="100">
                    </Image>
                </Canvas>
            </Grid>
        </ScrollViewer>
        <Border BorderBrush="#FFD0D0D0" BorderThickness="0,0,0,2"/>
        <Grid x:Name="LogoSettingsGrid" Height="120"  Background="#FF1F1F1F" VerticalAlignment="Bottom">

            <Border Background="#FF4D4D4D"  VerticalAlignment="Top" Grid.Row="0">
                <TextBlock x:Name="MaterialsListTextBlock" Text="Logo Settings" FontSize="24" FontWeight="Bold" Foreground="#FFD0D0D0" FontFamily="FiraSansBold" Margin="0,0,0,0" HorizontalAlignment="Center" />
            </Border>

            <Grid Margin="0,30,0,0"  >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="0.35*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Grid x:Name="SizeSliderGrid" Grid.Column="0">
                    <Label Content="Size" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="18"/>
                    <Slider x:Name="SizeSlider" Margin="25,45,25,0" Minimum="10" Maximum="200" Value="100" TickPlacement="TopLeft" ValueChanged="SizeSlider_ValueChanged"/>
                    <Label Content="10%" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18" Margin="20,15,0,0"/>
                    <Label Content="200%" Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="18" Margin="0,15,10,0" HorizontalContentAlignment="Right"/>
                </Grid>
                <Grid Grid.Column="1">
                    <Button x:Name="ResetButton" Background="#FF555555" Foreground="White" FontFamily="Nunito Black" HorizontalAlignment="Center" BorderBrush="{x:Null}" HorizontalContentAlignment="Center" VerticalAlignment="Center" Click="ResetButton_Click">
                        <Grid Width="Auto">
                            <TextBlock x:Name="ResetTextBlock" FontFamily="Nunito Black" Text="Reset Logo Position" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>
                </Grid>
                <Grid x:Name="TransparencySliderGrid" Grid.Column="2">
                    <Label Content="Transparency" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    <Slider x:Name="TransparencySlider" Margin="25,45,25,0" Minimum="0" Maximum="100" Value="100" TickPlacement="TopLeft" ValueChanged="TransparencySlider_ValueChanged"/>
                    <Label Content="0%" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18" Margin="20,15,0,0"/>
                    <Label Content="100%" Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="18" Margin="0,15,10,0" HorizontalContentAlignment="Right"/>

                </Grid>
            </Grid>
        </Grid>


    </Grid>
</Window>
